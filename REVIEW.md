# LocalTea Project - Полный Код-Ревью

## Оглавление
1. [Общее описание проекта](#общее-описание-проекта)
2. [Архитектура](#архитектура)
3. [Безопасность](#безопасность)
4. [Backend API](#backend-api)
5. [Admin Backend](#admin-backend)
6. [Admin Frontend](#admin-frontend)
7. [База данных и модели](#база-данных-и-модели)
8. [Тестирование](#тестирование)
9. [DevOps и инфраструктура](#devops-и-инфраструктура)
10. [Рекомендации по улучшению](#рекомендации-по-улучшению)
11. [Критические проблемы](#критические-проблемы)

---

## Общее описание проекта

LocalTea — это e-commerce платформа для продажи чая со следующими компонентами:
- **Backend API** — основной API для пользователей (FastAPI)
- **Admin Backend** — API для админ-панели (FastAPI)
- **Admin Frontend** — веб-интерфейс админ-панели (Next.js + Mantine)
- **Celery Worker** — фоновые задачи
- **PostgreSQL** — основная база данных
- **Redis** — кэш и очередь задач

---

## Архитектура

### Плюсы ✅
1. **Чёткое разделение на модули** — отдельные директории для API, CRUD, моделей, схем и сервисов
2. **Использование асинхронного SQLAlchemy** — хорошая производительность для I/O операций
3. **Dependency Injection** — правильное использование FastAPI Depends
4. **Отдельный Admin Backend** — изоляция административного функционала
5. **Celery для фоновых задач** — правильный подход для email-рассылок и периодических задач
6. **Rate Limiting** — защита от DDoS через slowapi

### Минусы ❌
1. **Дублирование кода** между backend и admin_backend (например, deps.py)
2. **Отсутствие общих пакетов** — можно было бы вынести общие модели в shared package
3. **Смешение бизнес-логики в endpoints** — некоторые endpoints содержат логику, которая должна быть в сервисах

---

## Безопасность

### Что сделано хорошо ✅

1. **Хеширование паролей (Argon2)**
   ```python
   pwd_context = CryptContext(schemes=["argon2"], deprecated="auto")
   ```
   Argon2 — лучший выбор для хеширования паролей.

2. **JWT с ротацией refresh токенов**
   - Access токены короткоживущие (30 минут)
   - Refresh токены с ротацией при обновлении
   - Хеширование refresh токенов в БД

3. **CSRF защита**
   ```python
   def create_csrf_token() -> str:
       nonce = secrets.token_hex(32)
       signature = hmac.new(...)
       return f"{nonce}.{signature}"
   ```

4. **2FA для админов (TOTP)**
   - Шифрование TOTP секретов через Fernet
   - Pre-auth токены для двухэтапной авторизации

5. **Rate Limiting**
   ```python
   @limiter.limit("2/minute")
   @limiter.limit("10/hour")
   async def registration(...)
   ```

6. **Fingerprint checking при refresh**
   - Проверка IP и User-Agent
   - Отзыв токена при изменении контекста

7. **Разделение прав в PostgreSQL** (init.sql)
   - migration_user для миграций
   - app_user только для DML операций

### Проблемы безопасности 🔴

#### 1. **Hardcoded IP в коде** (КРИТИЧНО)
```python
# backend/services/user.py
"link": f"http://5.129.219.127:8000/api/v1/user/confirm-email?token={...}"
```
**Решение:** Вынести в переменные окружения.

#### 2. **secure=False в cookies** (КРИТИЧНО для production)
```python
response.set_cookie(
    key="refresh_token",
    ...
    secure=False  # Set to True in prod
)
```
**Решение:** Использовать `settings.DEBUG` или отдельную переменную.

#### 3. **Отсутствие проверки IP в webhooks** (СРЕДНЯЯ)
```python
# backend/api/v1/webhooks/endpoints.py
# IP Whitelist check could be here...
```
**Решение:** Добавить проверку IP-адресов Yookassa.

#### 4. **Логирование токенов** (НИЗКАЯ)
```python
# admin_backend/core/deps.py
logger.info(f"[AUTH] Token received: {token[:20]}...")
```
В production лучше не логировать даже часть токена.

#### 5. **Hardcoded пароли в init.sql** (СРЕДНЯЯ)
```sql
CREATE USER migration_user WITH PASSWORD 'migration_password';
```
**Решение:** Использовать environment variables или Docker secrets.

#### 6. **Отсутствие валидации файлов** (СРЕДНЯЯ)
```python
# admin_backend/services/image_service.py
img = Image.open(file.file)  # Нет проверки размера до открытия
```
**Решение:** Добавить проверку размера файла до обработки.

---

## Backend API

### Структура эндпоинтов

| Модуль | Путь | Описание |
|--------|------|----------|
| User | /api/v1/user | Регистрация, авторизация, профиль |
| Catalog | /api/v1/catalog | Категории, продукты, SKU |
| Cart | /api/v1/cart | Корзина |
| Orders | /api/v1/orders | Заказы |
| Blog | /api/v1/blog | Статьи |
| Interactions | /api/v1/interactions | Лайки, комментарии, просмотры |
| Webhooks | /api/v1/webhooks | Платёжные webhooks |

### Что хорошо ✅

1. **Гибкая система корзины**
   - Поддержка анонимных и авторизованных пользователей
   - Session ID через header
   - Merge корзин при авторизации

2. **Атомарные операции со stock**
   ```python
   stmt = (
       update(SKU)
       .where(SKU.id == item_data["sku_id"])
       .where(SKU.quantity >= item_data["quantity"])
       .values(
           quantity=SKU.quantity - item_data["quantity"],
           reserved_quantity=SKU.reserved_quantity + item_data["quantity"]
       )
   )
   ```

3. **Автоматическая отмена неоплаченных заказов**
   ```python
   celery_app.conf.beat_schedule = {
       "check-expired-orders-every-minute": {
           "task": "backend.worker.check_expired_orders",
           "schedule": 60.0,
       },
   }
   ```

4. **Redis кэширование счётчиков**

### Что можно улучшить 🟡

1. **Пагинация через курсоры** вместо offset
2. **Bulk операции** для обновления нескольких позиций корзины
3. **Webhooks с подписью** — проверка подписи Yookassa

---

## Admin Backend

### Плюсы ✅

1. **Двухфакторная аутентификация**
2. **Audit логирование** всех действий админов
3. **Отдельный Dockerfile**

### Минусы ❌

1. **Refresh токен хранится без хеша**
   ```python
   # admin_backend/api/v1/auth.py
   db_token = DBToken(
       ...
       refresh_token=refresh_token_str,  # Plain text!
   )
   ```
   В основном backend используется хеширование, здесь нет.

2. **TODO в коде**
   ```python
   user_agent="Admin Panel", # TODO: Get from request
   ip="0.0.0.0" # TODO: Get from request
   ```

---

## Admin Frontend

### Плюсы ✅

1. **Современный стек**: Next.js 16, React 19, Mantine UI
2. **TypeScript** для типобезопасности
3. **Автоматическая генерация API клиента** из OpenAPI
4. **Zustand** для state management
5. **React Query** для серверного состояния

### Минусы ❌

1. **Refresh токен в localStorage** (менее безопасно, чем httpOnly cookie)
2. **Нет offline поддержки**

---

## База данных и модели

### Модели

| Модель | Описание |
|--------|----------|
| User | Пользователи |
| Token | Сессии с refresh токенами |
| Category | Категории товаров |
| Product | Товары |
| SKU | Варианты товаров (вес, цена) |
| ProductImage | Изображения товаров |
| Cart, CartItem | Корзина |
| Order, OrderItem, Payment | Заказы |
| Article | Статьи блога |
| Comment, Like, View, Report | Взаимодействия |

### Индексы ✅
- Все основные поля проиндексированы
- Составной индекс на `(status, expires_at)` для заказов

### Ограничения (Constraints) ✅
```python
__table_args__ = (
    CheckConstraint('num_nonnulls(user_id, session_id) = 1', name='check_cart_owner'),
)
```

### Рекомендации
1. Добавить **partial индексы** для часто используемых фильтров:
   ```sql
   CREATE INDEX ix_order_active ON "order" (status) WHERE status = 'awaiting_payment';
   ```

2. Добавить **GIN индекс** для полнотекстового поиска:
   ```sql
   CREATE INDEX ix_product_search ON product USING GIN(to_tsvector('russian', title || ' ' || description));
   ```

---

## Тестирование

### Что есть ✅
- Unit тесты для User API
- Фикстуры для создания пользователей
- Мокирование Redis и Celery
- Pytest-asyncio для асинхронных тестов

### Что нужно добавить ❌
1. **Integration тесты** для Order flow
2. **E2E тесты** для критических путей
3. **Тесты Admin API**
4. **Тесты безопасности** (OWASP ZAP, Bandit)

### Покрытие
Текущие тесты:
- test_user_api.py — авторизация, регистрация, профиль
- test_cart_api.py — корзина
- test_catalog_api.py — каталог
- test_order_api.py — заказы
- test_interactions_api.py — лайки, комментарии

---

## DevOps и инфраструктура

### Docker ✅
- Multi-stage build (dev/prod)
- Удаление shell в prod для безопасности
- Non-root пользователь (UID 10001)

### Docker Compose
- Сервисы: backend, admin_backend, worker, admin_frontend, db, redis
- Volumes для персистентности
- DNS настройки

### Что можно улучшить
1. **Health checks** в docker-compose
2. **Resource limits** (memory, CPU)
3. **Secrets management** через Docker secrets или Vault
4. **CI/CD pipeline** (GitHub Actions / GitLab CI)

---

## Рекомендации по улучшению

### Приоритет 1 (Критично)

1. **Исправить hardcoded URLs**
   - Все URL должны браться из environment variables

2. **Включить secure cookies в production**
   ```python
   secure = not settings.DEBUG
   ```

3. **Хешировать refresh токены в admin_backend**

4. **Добавить IP whitelist для webhooks**

### Приоритет 2 (Важно)

1. **Добавить Content-Security-Policy headers**
2. **Реализовать rate limiting на уровне nginx**
3. **Добавить monitoring (Prometheus + Grafana)**
4. **Настроить backup БД**

### Приоритет 3 (Желательно)

1. **Вынести общий код в shared package**
2. **Добавить API versioning strategy**
3. **Реализовать graceful shutdown**
4. **Добавить OpenTelemetry tracing**

---

## Критические проблемы

### 1. Hardcoded Production URL
**Файл:** `backend/services/user.py`
```python
"link": f"http://5.129.219.127:8000/api/v1/user/confirm-email?token={...}"
```

### 2. Cookies без secure flag
**Файл:** `backend/api/v1/user/endpoints.py`
```python
secure=False  # Set to True in prod
```

### 3. Refresh токен без хеширования в Admin
**Файл:** `admin_backend/api/v1/auth.py`

### 4. Incomplete endpoint implementation
**Файл:** `backend/api/v1/interactions/endpoints.py`
```python
@router.delete("/comments/{comment_id}", status_code=204)
async def delete_comment(...):
    # TODO: Implement permission check (author or admin)
    pass  # <-- Ничего не делает!
```

---

## Заключение

**Общая оценка: 7/10**

Проект имеет хорошую архитектуру и использует современные практики разработки. Основные проблемы связаны с:
- Hardcoded значениями, которые должны быть в конфигурации
- Неполной реализацией некоторых эндпоинтов
- Небольшими несоответствиями в безопасности между backend и admin_backend

После исправления критических проблем проект будет готов к production deployment.

---

*Дата ревью: 2024*
*Ревьюер: GitHub Copilot*
